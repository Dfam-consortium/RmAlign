// nextflow.config

manifest {
    name        = 'RmAlign'
    version     = '0.1.0'   // mirrors _version.py — update both on release
    description = 'Family-wise transposable element alignment pipeline'
    author      = 'Robert Hubley'
}

includeConfig 'conf/base.config'
includeConfig 'conf/profiles.config'
